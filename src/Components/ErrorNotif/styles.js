import styled from "styled-components";

export const ErrorNotifContainer = styled.section`
  width: 100%;
  min-height: 64px;
  background-color: var(--primary-danger);
  color: var(--dim-danger);
  display: flex;
  justify-content: center;
  align-items: center;
`;
